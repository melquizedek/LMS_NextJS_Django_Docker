'use client';

import React from 'react';
import { useNotification } from '@/hooks/useNotification';

export function Toast() {
  // Toast container is managed inside NotificationProvider
  return null;
}

export default Toast;
